const { question } = require("readline-sync")

var A = question("Digite o número que representa 'A': ")
var B = question("Digite o número que representa 'B': ")

console.log(`Os numeros (${A},${B}) escrito de forma inversa é (${B},${A})`)